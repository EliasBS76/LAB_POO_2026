¿Qué diferencia hay entre JDK , JRE Y JVMM, ¿Por qué instalamos el JDK?
    R: eL JDK como sus siglas lo mencionan es el kit de desarollo de java que desarolla aplicaciones
    El JRE es como un ambiente para poder correr las aplicaciones creadas en java
    JVM este tiene la función de ejecutarlo en cualquier dispositivo

    Usamos el JDK por el motivo que este nos permite construir los programas y los demas son solo herramientas para ejecutarlos

¿Por qué Java se considera «escribir una vez, ejecutar en cualquier lugar»? Explica con tus propias palabras.
    R: Esto se debe ya que al momento de compilar este no se ve afectado o alterado por el sistema operativo en el que este corriendo
    si no más bien que usa una conexión llamada bytecode sin necesidad de adaptar el programa en cada sistema.

¿Qué hace exactamente =System.out.println()=? ¿Qué clase y método estás usando?
    R: Imprime texto en la consola e inserta un salto de linea al final de cada uso , es un metodo de la clase PrintStream y el metodo es println